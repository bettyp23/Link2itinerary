# Link2itinerary
Link2Itinerary is a TypeScript-based web app (React + NestJS + Postgres) that turns user-provided social travel links and summaries into validated, budget-aware itineraries using a simple LLM planner plus a deterministic consistency engine.
